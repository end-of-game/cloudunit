---
spring:
  profiles: vagrant
  data:
    mongodb:
      host: 192.168.50.4
      database: cu-manager-domain
  rabbitmq:
    host: 192.168.50.4
server:
  port: 8080
logging:
  level:
    org.springframework.data: debug
cloudunit:
  orchestrator:
    base-uri: http://localhost:8081/
---
spring:
  profiles: local
  data:
    mongodb:
      host: localhost
      database: cu-manager-domain
    rabbitmq:
      host: localhost
server:
  port: 8080
logging:
  level:
    fr.treeptik: debug
cloudunit:
  orchestrator:
    base-uri: http://localhost:8081/
---
spring:
  profiles: production
  data:
    mongodb:
      host: cu-mongo
      database: cu-manager-domain
    rabbitmq:
      host: cu-rabbitmq
server:
  port: 8080
logging:
  level:
    fr.treeptik: info
cloudunit:
  orchestrator:
    base-uri: http://cu-docker-orchestrator:8081/
