sudo: required
language: java

services:
  - docker
  - maven


before_install:
  - docker version

script:
- cd cu-services && ./build-services.sh all
- docker images
- ./check_build_images.sh
- pwd
- cd /home/travis/build/Treeptik/cloudunit &&  mvn install -DskipTests

