version: '2'
services:
db:
  image: mongo
  ports:
    -"27017-27017"
#  command: "--smallfiles --logpath=/dev/null"
web:
  build: .
  command: nodemon index.js
  volumes:
    - .:/testing-cloudunit
  ports:
    - "3000:3000"
  environment: 3000
